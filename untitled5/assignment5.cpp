//
// Created by aidan on 11/27/18.
//
#include <iostream>
#include <fstream>
#include <sstream>
#include <vector>

#include "word.h"
#include "binarySearchTree.h"
int main(){
    /*binarySearchTree<word> english;
    std::string currentLined;
    std::ifstream dictionary;
    dictionary.open("english_words.txt");

    while (dictionary>>currentLined)
    {
        word nextinline(currentLined);
        english.insert(nextinline);


    }
    std::cout<<"Test of find words\n\n";
    std::cout<<"The dictionary has been built with "<< english.size() <<" keys in the dictionary\n";
    std::cout<<"Word to unscramble (quit-now to stop):"<<std::endl;
    std:: string scrambled;
    std::cin>>scrambled;
    while(scrambled != "quit-now") {
        std::cout << "Word to unscramble (quit-now to stop):" << std::endl;
        std::cin >> scrambled;
        std::ifstream scrambledwords;
        scrambledwords.open("scrambled_words.txt");
        while (getline(scrambledwords, scrambled))
        {
            word fscrambled(scrambled);
            english.find(fscrambled, &english.processFound);
        }
    }*/
    int *p;



    return 0;
}
